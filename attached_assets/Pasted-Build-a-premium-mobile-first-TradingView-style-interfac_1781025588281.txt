Build a premium mobile-first TradingView-style interface using React, Tailwind CSS v4, Motion v12 ("motion/react"), and GPU-accelerated animations.

The UI style must follow the 2026 "Liquid Glass Bento Grid" aesthetic with ultra-smooth 60FPS interactions, dynamic blur effects, floating panels, and high-end mobile UX.

Main Goal:
Recreate advanced animated behavior specifically for:

1. Bottom floating control bar (mobile mode)
2. Main left-side drawing/tools control panel
3. TradingView-like interactions
4. Smooth morphing animations between minimized and expanded states

---

GLOBAL DESIGN SYSTEM

Theme Style:

- Premium dark glassmorphism UI
- Deep obsidian background: #0A0A0A
- Ambient animated gradients:
  - Indigo glow
  - Cyan blur
  - Violet accents
- Floating layered depth using:
  - backdrop-blur-xl
  - bg-white/[0.06]
  - border-white/[0.12]
  - shadow-[0_0_40px_rgba(99,102,241,0.15)]

Typography:

- Use "Geist" or "Inter"
- Tight spacing
- Semi-bold labels
- Ultra clean modern appearance

Performance:

- Use transform-gpu everywhere
- Avoid layout thrashing
- Use will-change-transform
- All animations must stay 60FPS on Android mobile devices

---

MOBILE BOTTOM CONTROL BAR

Create a floating bottom control bar similar to TradingView mobile UI.

Requirements:

- Position fixed bottom center
- Rounded full pill shape
- Glassmorphism effect
- Floating above chart with soft shadow glow
- Width adaptive
- Smooth blur background

Initial State:

- opacity: 0
- y: 100
- scale: 0.92
- blur: 10px

Animate In:

- spring animation
- stiffness: 180
- damping: 24
- mass: 0.9

Animation Sequence:

1. Bar slides upward
2. Blur sharpens
3. Icons stagger fade-in
4. Active tab glows

Motion Variants:
hidden:
{
opacity: 0,
y: 100,
scale: 0.92,
filter: "blur(10px)"
}

visible:
{
opacity: 1,
y: 0,
scale: 1,
filter: "blur(0px)",
transition: {
type: "spring",
stiffness: 180,
damping: 24,
staggerChildren: 0.04
}
}

---

BOTTOM BAR INTERACTIONS

Each button should:

- Have magnetic hover/touch response
- Scale slightly on tap
- Produce soft liquid glow
- Use radial cursor-follow lighting

whileTap:
{
scale: 0.94
}

whileHover:
{
scale: 1.08,
y: -2
}

Active Button:

- Indigo neon glow
- Soft animated background pulse
- Backdrop blur intensifies to 24px

---

LEFT SIDE CONTROL PANEL

Create a floating expandable left-side drawing tools panel.

Collapsed State:

- Slim vertical floating pill
- Only icons visible
- Semi-transparent glass

Expanded State:

- Morph into wider panel
- Smooth width animation
- Labels fade in
- Background blur intensifies
- Nested controls slide in staggered

Animation:

- Use shared "layoutId"
- Morph animation between collapsed and expanded states
- Spring easing

Collapsed Width:
72px

Expanded Width:
320px

Panel Motion:
initial:
{
x: -80,
opacity: 0
}

animate:
{
x: 0,
opacity: 1,
transition: {
type: "spring",
stiffness: 140,
damping: 22
}
}

---

DRAWING TOOL INTERACTIONS

When user selects:

- Trendline
- Fibonacci
- Rectangle
- Brush
- Long position
- Short position

Then:

1. Selected tool glows
2. Floating mini toolbar appears beside selected tool
3. Mini toolbar morphs from selected icon position
4. Smooth blur transition
5. Tool settings fade upward staggered

Mini Toolbar Animation:

- scale: 0.8 → 1
- opacity: 0 → 1
- y: 10 → 0

Transition:
{
type: "spring",
stiffness: 220,
damping: 18
}

---

FLOATING MINI CONTROL BAR

When chart element is selected:

- Original bottom bar smoothly transforms into contextual mini toolbar
- Use shared layout transitions
- Avoid instant disappearance

IMPORTANT FIX:
Prevent touch propagation issues where:

- Selecting toolbar buttons accidentally triggers underlying controls

Implement:

- pointer-events isolation
- z-index layering system
- touch-action manipulation
- stopPropagation on floating controls

---

CHART EXPERIENCE

Requirements:

- Ultra smooth pan and zoom
- GPU accelerated transforms
- Inertial dragging
- Momentum scrolling
- No lag while zooming

Use:

- requestAnimationFrame optimizations
- transform3d
- memoized rendering
- virtualized redraw updates

Live Price Updates:

- Tick-by-tick smooth animation
- No hard jumps
- Interpolate price movement

Candles:

- Smooth updates
- Animated wick growth
- Soft glow on active candle

---

BACKGROUND EFFECTS

Animated Mesh Gradient:

- Slow moving
- Very subtle
- Indigo + cyan + violet

Noise Texture:

- Extremely subtle grain overlay

Glass Refraction:

- Use pseudo-elements
- Dynamic reflection layer
- Slight animated shimmer

---

MODAL / PANEL TRANSITIONS

Use AnimatePresence everywhere.

Any popup/modal:

- Must morph from source element
- Shared layout animation
- Blur background smoothly
- Fade + scale transition

Transition:
{
type: "spring",
stiffness: 160,
damping: 20
}

---

TECHNICAL REQUIREMENTS

Stack:

- React
- Tailwind CSS v4
- motion/react
- TypeScript

Architecture:

- Separate all motion variants into:
  "/animations/variants.ts"

Hooks:

- useMotionValue
- useSpring
- useTransform
- useReducedMotion

Accessibility:

- Respect prefers-reduced-motion

Optimization:

- Lazy load heavy panels
- Avoid unnecessary rerenders
- Use React.memo
- Use dynamic imports

---

FINAL FEELING / VIBE

The UI should feel:

- Premium
- Cinematic
- Fluid
- Luxurious
- TradingView + Apple VisionOS + Linear inspired

NOT:

- Cheap
- Over-bouncy
- Cartoonish
- Laggy

Use easing:
cubic-bezier(0.22, 1, 0.36, 1)

for all non-spring animations.

Final result should look like a futuristic professional trading platform built for elite traders in 2026.