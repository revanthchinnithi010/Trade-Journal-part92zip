Fix TradingView-style mouse + touch interaction system completely.

Current behavior:
- Touch gestures work correctly:
  - smooth 360° free movement
  - left/right
  - up/down
  - zoom
- But mouse interaction is broken:
  - moving mouse crosshair also moves chart
  - chart pan activates unintentionally
  - desktop TradingView behavior is not matched

I want EXACT TradingView desktop + mobile interaction behavior.

Required behavior:

DESKTOP MOUSE:
1. Mouse move:
   - ONLY move crosshair
   - NEVER pan chart
   - NEVER affect price scale
   - NEVER affect visible range

2. LEFT mouse click + drag:
   - pan chart freely
   - smooth 360° movement
   - no jitter
   - no accidental zoom

3. Mouse wheel:
   - zoom smoothly

4. Price scale drag:
   - ONLY when dragging inside price scale area

5. Time scale drag:
   - ONLY when dragging bottom time axis

6. Crosshair must feel completely independent.

MOBILE TOUCH:
1. One finger drag:
   - smooth free-hand pan
   - 360° movement
   - ultra smooth

2. Tap:
   - crosshair only

3. Hold + move:
   - crosshair tracking only

4. Pinch:
   - zoom only

EVENT SYSTEM FIX:
1. Separate states:
   - crosshairTracking
   - chartPanning
   - priceScaleDragging
   - timeScaleDragging
   - pinchZooming
   - drawingDragging

2. NEVER allow simultaneous states.

3. Add proper gesture thresholds.

4. pointermove must NEVER trigger chart transform automatically.

5. Only apply pan transforms after:
   mousedown + drag threshold exceeded

6. Remove all accidental delta accumulation.

7. Prevent passive listener conflicts.

8. Use requestAnimationFrame() for:
   - crosshair updates
   - smooth pan rendering

9. GPU accelerate:
   - transforms
   - panning
   - zooming

10. Remove all lag/jitter.

11. Match TradingView exactly:
   Desktop:
   - hover = crosshair only
   - drag = pan

   Mobile:
   - touch drag = pan
   - tap = crosshair

12. Ensure chart movement physics feel premium and ultra responsive.

13. Fix mouse-specific event conflicts:
   - pointermove
   - mousemove
   - dragstart
   - pointercapture
   - hover state
   - wheel state

14. Ensure crosshair NEVER moves chart again.