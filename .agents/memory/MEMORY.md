- [cTrader trendbars on live session](ctrader-trendbars-session.md) — fetchTrendbarsOnSession() on CtraderTickEngine uses the live streaming TLS session; avoids new connection + re-auth per candle request; FIFO pendingTrendbarsQueue routes GET_TRENDBARS_RES(2138) back to callers; candles.ts prefers this over standalone fetchTrendbars
- [Charts terminal architecture](charts-terminal.md) — all chart work lives in artifacts/trading-journal/src/pages/charts.tsx + src/components/charts/* only; never a separate artifact
- [Drawings deletion persistence](drawings-deletion.md) — deleted drawing IDs stored in localStorage; must filter on API load via resetDrawings() not setDrawings()
- [Chart settings architecture](chart-settings.md) — ChartSettings interface in SettingsPanel.tsx; applied reactively to lightweight-charts via useEffect in CustomChart.tsx; settings passed as prop
- [Multi-chart grid height fix](multi-chart-grid-height.md) — CSS grid 1fr rows need explicit container height; use position:absolute inset:0 inner div, not flex:1 outer div, as grid root
- [Global alert store](global-alert-store.md) — unified Zustand store at src/store/alertStore.ts; LS key "tj_global_alerts_v1"; all four alert surfaces share it
- [Position drawing tool](position-tool.md) — points[0]=entry+leftX, points[1]=TP+rightX, points[2]=SL(priceOnly); SL anchor locks X via toolType in DragState; 4 square handles; min-width fallback 240px
- [Position tool right-edge normalization](position-tool-right-edge.md) — right edge must be saved as entry.time + N×intervalSec; un-normalized future timestamps drift as new candles form; normalize at save, handle-13 drag commit, and move-drag commit; use timeframeRef to avoid restarting drag useEffect
- [Vertical pan architecture](vert-pan.md) — autoScale:true kills LWC vertTouchDrag; use capture-phase listeners on containerRef for custom vert pan via asymmetric scaleMargins
- [Vertical pan + indicator union fix](vert-pan-indicator-union.md) — LWC unions all pane-0 series' autoscaleInfoProvider ranges; chartPanState.ts pub-sub fixes by locking ALL series to the same range
- [Drawing drag — direct DOM architecture](drawing-drag-dom.md) — move drag uses SVG transform on <g data-did> wrapper (zero React renders); anchor drag uses RAF+setDragTick; DragState caches overlayRect+startPxPoints at drag start
- [Touch anchor drag — pointer capture on stable element](touch-anchor-capture.md) — setPointerCapture must target overlayRef.current (stable div), NOT the SVG anchor circle; circle is destroyed/recreated by setDragTick re-renders, silently releasing capture mid-drag on touch
- [ColorBox picker registry — Symbol Map pattern](colorbox-registry.md) — use Map<symbol,fn> not Set<fn>; function-reference except checks always fail when wrappers are registered; also never call _closeAllColorBoxes inside a setState updater
- [LivePriceBox RAF architecture](live-price-box-raf.md) — RAF loop directly mutates DOM refs at 60fps; all hot-path values in a single rc ref object; no useState for yPos/cd/color; no subscribeVisibleTimeRangeChange subscription needed
- [Zero-latency tick path](tick-latency.md) — tickDataRef written by WS handler before any setState; RAF loop reads tickDataRef.current instead of Zustand state; Zustand capped at ≤1 setState per rAF frame
- [Incremental EMA/VWAP on live tick](incremental-ema.md) — O(1) per tick using emaPrevRef/emaCurrRef; fillIndicator seeds the cache; new-bar detection via emaLastBarTimeRef; VWAP uses cumulative ref
- [Position toolbar architecture](position-toolbar.md) — PositionToolbar.tsx is a specialized portal-based toolbar for position_long/position_short; DrawingStyle has profitColor/stopColor (hex); hexToRgba() converts at render time; API PATCH supports toolType for reverse
- [Position tool coordinate sync](position-tool-sync.md) — 3 invariants: setChartCtx after applyBars; candleBars memo needs replayBarCount dep; subscribe livePrice for tick-driven re-renders
- [Position tool viewport future-space](position-tool-viewport.md) — fitContent() saves to≈numBars with zero future space; position right edges go off-screen; fix: guarantee MIN_FUTURE_BARS=50 and call setChartCtx AFTER viewport setup
- [Mobile responsive switching](mobile-responsive.md) — Charts() has early return for mobile using useIsMobile(); all hooks must precede it; inline hook calls inside JSX (like useAlertStore()) must be hoisted before the early return
- [Orientation-based isMobile](orientation-ismobile.md) — use matchMedia("(orientation: portrait)") NOT screen.orientation.type; the latter reports physical screen orientation (always landscape on desktop), matchMedia respects viewport
- [Delta Exchange OAuth architecture](delta-oauth.md) — Delta uses OAuth 2.0 popup flow; routes at /api/delta/oauth/*; access_token in api_key_enc, refresh_token in api_secret_enc; meta JSONB column stores auth_mode+expires_at; BrokerService reads meta to pass DeltaAuthMode to adapter; requires DELTA_CLIENT_ID + DELTA_CLIENT_SECRET env vars
- [Broker WS manager architecture](broker-ws-manager.md) — modular src/lib/broker-ws/; DeltaWsClient (direct WS), LivePnlTracker (O(1) per tick); orchestrated by BrokerWsOrchestrator; mounted via useBrokerWs in Layout
- [Delta Exchange API Key architecture](delta-api-key-arch.md) — NO OAuth; HMAC SHA256 api_key flow; deltaSigner+deltaAuth services; deltaSocketManager relays private WS; BrokerConnectModal+DeltaApiConnectForm glassmorphism UI
- [Delta Exchange environment auto-detection](delta-env-detection.md) — invalid_api_key = wrong endpoint (India vs International), not wrong credentials; probe both concurrently, store base_url+ws_url in meta
- [Broker store architecture](broker-store-architecture.md) — dual-name state fields (sync helpers), per-broker poll intervals, WS sync hook, reconnect backoff; livePnl + wsClientStates added for real-time tracking
- [Broker encryption key](broker-encryption-key.md) — BROKER_ENCRYPTION_KEY must be in Replit Secrets; absent key → 401 "reconnect required"; SHA-256 derivation + legacy slice fallback in BrokerEncryption.ts
- [Credential import system](credential-import.md) — AppConfigService (AES-256-CBC, app_config table) + /api/credentials/* routes; injectToEnv() at startup; DeltaApiConnectForm shows one-click button when imported; ConnectionStatusPanel + CredentialImportModal on brokers.tsx
- [Per-client candle subscription](per-client-candle-sub.md) — WSManager tracks clientState Map; client sends subscribe_candles; broadcastCandleUpdate filters per key; payload cached once per interval; eliminates 89% candle WS traffic
- [Realtime pipeline architecture](realtime-pipeline.md) — ultraFastTickEngine.ts (ring buffer O(1), RAF dispatch) + websocketDispatcher.ts (typed router); LiveMarketContext exposes sendMessage(); CustomChart sends subscribe_candles on mount + symbol/interval change + welcome reconnect
- [Delta India all_trades channel](delta-all-trades.md) — correct WS channel is all_trades (NOT recent_trade); recent_trade fires zero events; all_trades confirmed working sub-second on Delta India; subscribe both all_trades + v2/ticker as baseline fallback
- [Client-side OHLC aggregation pattern](client-ohlc-aggregation.md) — MT5/TradingView pattern: chart tick handler in subscribeToMessages handles type=tick before candle_update; RealtimeTradeAggregator (realtimeTradeAggregator.ts) builds OHLC O(1); seeded from last historical bar in loadCandles; reset in subscribe_candles useEffect on symbol/interval change
- [Zero-React tick and crosshair architecture](zero-react-perf.md) — ticks: Zustand tickStore (src/store/tickStore.ts) not React context; crosshair: module pub-sub (src/lib/crosshairState.ts); OHLCV bar + price cells use DOM refs + RAF; LiveMarketContext retains only wsStatus/alertEvents/subscribeToMessages/sendMessage
- [DrawingOverlay Canvas2D architecture](drawing-canvas-arch.md) — all visual paths on a Canvas2D layer (drawingCanvasRenderer.ts); SVG retains only anchor handles + hit areas for selected drawings; toPxRef/inner component toPxRef renamed tbToPxRef to avoid clash; canvasOnly prop gates SVG path rendering
- [Position tool rendering layer](position-tool-render-layer.md) — position_long/short render ONLY in SVG; canvas loop must `continue` for these types or labels double-render (canvas+SVG both draw Target/Stop bars)
- [Chart drag-freeze + frame-coalescing](chart-drag-freeze.md) — scheduleChartUpdate() is the ONLY series.update() entry point; pendingChartBarRef holds latest bar; RAF suppressed during isInteractingRef; onUp flushes immediately; 30fps cap on touch via isTouchDeviceRef + lastRenderMsRef; both tick path and candle_update path funnel through this
- [Order execution backend architecture](order-exec-backend.md) — brokerAuth middleware (not JWT) attaches req.brokerCtx; per-account in-memory rate limiter; Zod validation on all order bodies; withRetry() exponential backoff in adapters; Delta supports Stop/StopLimit/bracket TP/SL
- [Multi-broker store architecture](multi-broker-store.md) — connectedAccounts Record<brokerId,BrokerAccount>; per-broker pollHandles Map; connect() adds without disconnecting others; disconnectBroker(id) removes one; deriveLegacy() keeps backward-compat activeAccount/connectionStatus/positions/orders/balance
- [Named layout system](named-layout-system.md) — useNamedLayouts hook (src/hooks/useNamedLayouts.ts), LS key tj_named_layouts_v1; saves symbol/interval/chartType/indicators/chartSettings/layoutCount; load via useChartStore.getState() setters + selectSymbol/selectInterval; UI embedded in LayoutSlide (desktop) + LayoutBottomSheet (mobile)
- [Market feed subscription queuing](feed-subscription-queuing.md) — MarketFeedManager.subscribe() must add to subscribedSymbols BEFORE checking provider existence; enableDelta uses subscribedSymbols to re-subscribe queued symbols when provider connects
- [Broker-aware symbol routing](broker-symbol-routing.md) — only "delta" provider remains; BrokerName = "delta" | "finnhub"; BrokerId = "delta" | "mt5" | "ctrader"
- [cTrader broker store auto-sync](ctrader-broker-sync.md) — reconcileAccount() in ctraderProtoOA.ts (ProtoOA 2124/2125) feeds /api/ctrader/balance|positions|orders; CtraderAdapter polls at 15s; CtraderWidget calls runFullAutoSetup() on OAuth success + connectToBrokerStore() on session restore
- [cTrader Markets integration](ctrader-markets.md) — Markets page has Delta/cTrader broker filter; cTrader: POST /api/ctrader/auto-setup (accounts→ProtoOA symbols→cache→start engine); categories from inferCtraderType in symbols.ts; bid/ask flows through tickStore now
- [brokerWatchlistStore — single source](broker-watchlist-store.md) — all watchlist state lives in src/store/brokerWatchlistStore.ts (Zustand); WatchlistContext.tsx is a thin bridge; useWatchlist() delegates to the store; chartStore.symbol is the selectedSymbol; Markets reads it for isActive highlight
- [Candle data source routing](candle-data-routing.md) — forex/index/commodity: Yahoo Finance REST only (Finnhub fully removed); crypto: Delta Exchange; Yahoo intervals were wrong ("1"→"2m","240"→"1d"), now fixed; 3m/2H/4H resample from finer resolution
- [cTrader ProtoOA transport](ctrader-protoa-transport.md) — port 5035 = raw TLS TCP (tls.connect), NOT WebSocket; ws library causes "Bye" close; confirmed APP_AUTH_RES over raw TLS
- [cTrader watchlist-driven subscriptions](ctrader-watchlist-subs.md) — engine starts with zero subs; subscribedIds Set drives all sub/unsub; watchlist add/remove in watchlist.ts calls addSymbol/removeSymbol; startup re-subscribes from DB via subscribeWatchlistCtraderSymbols()
- [SharedMarketSelector — unified symbol picker](shared-market-selector.md) — single component at src/components/SharedMarketSelector.tsx; mode="page" (markets.tsx) or mode="sheet" (Charts MiniControlBar); markets.tsx is a 20-line thin wrapper; old MarketWatchlistSheet + SymbolPickerSheet fully removed
- [chartStore as single source of truth](chartsore-single-source.md) — chartStore.symbol/interval are the ONLY source of truth; charts.tsx reads activeKey/interval via useChartStore selectors (not layout.symbol); setSymbol/setInterval persist to localStorage; store init writes defaults to localStorage at module load; SyncDiagnostics pill in both desktop+mobile confirms sync
